name: secure_coding
channels:
  - defaults
dependencies:
  - python=3.12
  - pip
  - pip:
    - flask 
    - flask-socketio
    - flask-sqlalchemy
    - flask-wtf
    - flask-limiter
    - flask-talisman
    - werkzeug